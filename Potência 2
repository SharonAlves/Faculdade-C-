#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main()
 {
 	int i,xmult;
 	for(i=1; i<20;i++){
 		printf("*");
	 }
	printf("\n");
	printf("10 potencias de 2 \n");
	for(i=1; i<20;
i++){
		printf("*");
	}
	for(i=1; i<11;i++){
		xmult = xmult*2;
		printf("2 elevado a %d: %d\n",i,xmult);
	}
	 for(i=1; i<20;i++){
	 	printf("*");
	
	 }
	return 0;
}
